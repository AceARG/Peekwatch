[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "peekwatch"
version = "0.1.0"
dependencies = ["rich", "scapy", "requests", "urllib3"]

[project.scripts]
peekwatch = "peekwatch:main"
